# Build context for the Quartz Dockerfile. The image copies the checkout with
# `COPY . .`, so keep repository state, local installs, build output, and npm
# configuration out of the context. Dependencies are installed inside the image.
.git
.github
node_modules
public
.quartz-cache
.npmrc
.env
.env.*
*.log
.DS_Store
Thumbs.db
